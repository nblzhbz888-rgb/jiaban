{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "@jiaban/electron-vueuse#build": {
      "dependsOn": ["@jiaban/electron-eventa#build"],
      "outputs": ["dist/**"]
    }
  }
}
